{{!< default}}

{{!-- Hero --}}
{{#page}}
  {{> page-hero}}
{{/page}}

<section class="px-5 sm:px-6" data-podcasts>
  <div class="max-w-site mx-auto border-t-3 border-brd-soft pt-8">
    <div class="grid gap-8 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 mb-8">
      {{#foreach posts}}
        {{> cards/podcast-card 
          excerpt_class="line-clamp-4"
        }}
      {{/foreach}}
    </div>

    {{> pagination }}
  </div>
</section>